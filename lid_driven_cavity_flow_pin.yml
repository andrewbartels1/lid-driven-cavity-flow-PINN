name: lid_driven_cavity_flow_pin
channels:
    - conda-forge
    - pyg
    - defaults
    - pytorch
# minimize what we download via conda forge for vast speedup
dependencies:
    - python
    - jupyter
    - cudatoolkit
    - pip
    - poetry